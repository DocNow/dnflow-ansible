---
  - name: install build environment
    apt: name={{ item }} state=present
    with_items:
      - build-essential
      - python3
      - python3-dev
      - python3-pip

  - name: pip3 upgrade from Ubuntu pip
    command: /usr/bin/pip3 install -U pip
